<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <Button
        android:id="@+id/adicionarConvidadoButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Adicionar Convidado" />

    <Button
        android:id="@+id/listarConvidadosButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Listar Convidados" />

    <Button
        android:id="@+id/buscarConvidadoButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Buscar Convidado por ID" />
</LinearLayout>
